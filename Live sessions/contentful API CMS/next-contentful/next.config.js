/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  env:{
    spaceId: 'piuof7tj3df1',
    environment : "master",
  },
};

module.exports = nextConfig
